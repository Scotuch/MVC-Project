/*
$(async function () {
    const veri = await istek(
        {
            id: "test",
            test: "deneme",
            islem: "test_api",
        },
        { bildirim: false, metod: "POST" }
    );
    console.dir(veri);
});
*/
